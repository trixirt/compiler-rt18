- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
      - classic
    required_packages:
      # the requirements below are for the integration suite
      - cmake
      - llvm-devel
      - clang
      - clang-devel
      - clang-analyzer
      - clang-tools-extra
      - compiler-rt
      - ninja-build
      - libcxx-devel
      - libstdc++-devel
      - libstdc++-static
      - libomp-devel
      - python-lit
      - lld
      - lldb
      - git
      - make
    tests:
      - integration-test-suite
